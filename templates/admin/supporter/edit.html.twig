{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="page-header">
        <div>
            <h1 class="page-title">
                <i class="fa-solid fa-hand-holding-heart" style="margin-right:8px;color:var(--bm-accent)" aria-hidden="true"></i>
                Editar Entidade de Apoio
            </h1>
            <p class="page-subtitle">Altere as informações da entidade de apoio.</p>
        </div>
        <a href="{{ path('admin_supporter_index') }}" class="btn-secondary">
            <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
            Voltar
        </a>
    </div>
        
    {{ include('admin/supporter/_form.html.twig') }}
    
    <form method="post" action="{{ path('admin_supporter_delete', {'id': supporter.id}) }}" onsubmit="return confirm('Tem certeza de que deseja excluir esta entidade de apoio?');" class="text-right mt-6">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ supporter.id) }}">
        <button class="btn-danger">
            <i class="fa-solid fa-trash" aria-hidden="true"></i>
            Excluir
        </button>
    </form>
{% endblock %}
