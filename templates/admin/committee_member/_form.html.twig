{{ form_start(form) }}
    <div class="admin-card space-y-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {{ form_row(form.name) }}
            {{ form_row(form.role) }}
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {{ form_row(form.institution) }}
            {{ form_row(form.groupType) }}
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {{ form_row(form.academicLink) }}
            {{ form_row(form.linkedinUrl) }}
        </div>
        <div>
            {{ form_row(form.bio) }}
        </div>
        <div>
            {{ form_row(form.image) }}
            {% if committee_member is defined and committee_member.image and committee_member.image.imageName %}
                <div class="mt-2">
                    <span class="text-xs text-slate-500 block mb-1">Visualização da foto atual:</span>
                    <img src="{{ asset('uploads/' ~ committee_member.image.imageName) }}" alt="Preview" class="w-32 h-32 object-cover rounded-full shadow border border-slate-200">
                </div>
            {% endif %}
        </div>
        {{ form_row(form.position) }}
    </div>
    
    <div class="w-full text-right mt-6">
        <button type="submit" class="btn-primary" id="submit-btn">
            <i class="fa-solid fa-check" aria-hidden="true"></i>
            Salvar
        </button>
    </div>
{{ form_end(form) }}